<h3>あなたの夢</h3>

            画像


<h3>目標一覧</h3>

<table>
  <tr>
    <th>目標ID</th>
    <th>目標</th>
    <th>ユーザー名</th>
  </tr>
  <% @goals.each do |goal| %>
    <tr>
      <td><%= goal.id %></td>
      <td><%= link_to goal.goal, goal_path(goal), class: 'btn btn-primary' %></td>
      <td><%= link_to goal.user.name, user_path(goal.user), class: 'btn btn-primary' %></td>
    </tr>
  <% end %>
</table>

<p>Find me in app/views/public/goals/index.html.erb</p>
