<%# app/views/public/relationships/followings.html.erb %>

<h2>フォローしているユーザー</h2>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user_path(user) %> <!-- ユーザーの詳細ページへのリンク -->
      </li>
    <% end %>
  </ul>
<% else %>
  <p>フォローしているユーザーはいません。</p>
<% end %>
